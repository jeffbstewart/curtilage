# Presubmit allowlist -- one pattern per line.
# Lines containing any of these substrings are excluded from violation
# reports; "file:<name>" skips a whole file.  Comments start with #.
# This repo is PUBLIC: keep this list short and justified.

# go.sum module hashes are base64, not hex, and never secrets.
file:go.sum
# The checker and its tests contain example IPs/keys by nature.
file:presubmit-check.sh
file:presubmit-check-test.sh
# Supply-chain pins ARE long hex by design: action commit SHAs in the
# workflow, image digests in the Dockerfile.
file:ci.yml
file:Dockerfile
# buf.lock pins the well-known-types module by commit and digest.
file:buf.lock
# Tests listen on loopback; that is not an address that leaks anything.
127.0.0.1:0
# The house page's tests are made of addresses (RFC 5737 / 1918 examples).
file:house_test.go
# Example config and schema comments use RFC 1918 / 5737 example ranges.
192.168.1.0/24
198.51.100.0/24
192.168.1.1
